import type { Options } from 'roughjs/bin/core';

const defaultConfig: Options = {
  roughness: 0.8,
  fill: '#FF5733',
  fillStyle: 'sunburst',
  fillWeight: 1,
  stroke: '#333',
  strokeWidth: 3
};

export default defaultConfig;
